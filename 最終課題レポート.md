# 『Physical AI講座 2026 応用編1』最終課題レポート

## 題目
**LOCAL-FIRST Natural Language Physical AI Robot**  
— 自然言語を理解し、CRANE+ V2の動作を選択するローカルAIロボット —

## 1. 概要
本課題では、ユーザーが日本語または英語で入力した自然言語の指示をローカルLLMが解釈し、
その推論結果に基づいてGazebo上のCRANE+ V2マニピュレータをROS 2で動作させる
Physical AIシステムを構築した。

AIにはOllama上で動作するQwen2.5 1.5Bを使用した。
AIは自然言語を WAVE / POINT_UP / BOW / REST の4種類の行動意図に分類し、
ROS 2ノードがその結果を `trajectory_msgs/msg/JointTrajectory` に変換して
`/crane_plus_arm_controller/joint_trajectory` へPublishする。
その後、ros2_controlを介してGazebo上のCRANE+ V2が実際に動作する。

## 2. 目的
固定コマンドを直接ロボットへ与えるのではなく、人間が日常的に使う自然言語からAIが意味を推論し、
その意味に応じてロボットが行動を選択する仕組みを実現することを目的とした。

「人間の指示 → AIの判断 → ROS 2による制御 → ロボットの行動」という
Physical AIの基本的な流れを、シミュレーション上で一体化して実装した。

## 3. システム構成
1. ユーザーが自然言語で指示を入力する。
2. ローカルLLM（Ollama / Qwen2.5 1.5B）が指示の意味を推論する。
3. AIが WAVE / POINT_UP / BOW / REST のいずれかを選択する。
4. ROS 2ノードが行動意図に対応したJointTrajectoryを生成する。
5. `/crane_plus_arm_controller/joint_trajectory` にPublishする。
6. ros2_controlが軌道指令を処理する。
7. Gazebo上のCRANE+ V2が動作する。

## 4. AIの利用
AIは単なる文章生成ではなく、ロボットの行動選択そのものに利用した。

例：
- 「こんにちは。元気よく挨拶してください」→ WAVE
- 「天井の方向を指してください」→ POINT_UP
- 「丁寧にお礼のおじぎをしてください」→ BOW
- 「休んでください」→ REST
- 「Hello! Please greet the audience.」→ WAVE

異なる言い回しでも、自然言語の意味をAIが判断してロボット行動へ変換する。

## 5. 工夫点

### 5.1 ローカルAI
LLMはWindows上のOllamaでローカル実行し、Docker上のROS 2ノードからアクセスする構成とした。
クラウドAPIに依存せず、ローカル環境内で自然言語推論を行う。

### 5.2 AIと制御の役割分離
LLMに任意の関節角を直接生成させず、
AIには「行動意図の分類」を担当させた。
実際の関節軌道はROS 2側で制約された候補から生成する。

これにより、
- AIの自然言語理解能力を利用できる
- 小型LLMでも結果を安定させやすい
- 予期しない関節値を直接送るリスクを抑えられる
という利点がある。

### 5.3 小型LLM向けの安定化
Qwen2.5 1.5Bは軽量でローカル実行しやすい一方、
短い指示では分類が揺れる場合がある。
そこでプロンプトに具体例を与え、
出力を WAVE / POINT_UP / BOW / REST / UNKNOWN に限定した。

### 5.4 講義内容の発展
講義で扱ったPublisher、JointTrajectory、ros2_control、Gazeboの流れを利用し、
その前段にAIによる自然言語理解を追加した。
これにより、ROS 2通信実習をPhysical AIへ発展させた。

## 6. 動画で示す内容
動画ではGazeboとターミナルを同時に表示し、
以下を確認できるようにする。

1. 自然言語の指示
2. AIの判定結果
3. JointTrajectoryのPublishログ
4. Gazebo上のCRANE+ V2の実際の動作

日本語の複数表現と英語指示を含め、
固定文字列判定ではなくAIによる意味解釈を利用していることを示す。

## 7. 今後の発展
今後はカメラ画像をVLMへ入力し、
視覚情報と自然言語を統合して
「見る → 考える → 動く」マルチモーダルPhysical AIへ発展させたい。

さらに物体認識結果やロボット状態をAIへフィードバックし、
環境に応じて行動を再計画する閉ループ制御への拡張も考えられる。
